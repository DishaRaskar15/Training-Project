# Training-Project
ShopSphere – A full-stack e-commerce web app built with HTML/CSS/JS, FastAPI &amp; Supabase. Features JWT auth, product catalog, cart (ArrayList DS), undo-remove (Stack DS), wishlist (HashSet) &amp; category filter (HashMap). REST APIs tested via Postman. Data persisted in PostgreSQL (Supabase).
